// Throwaway: end-to-end smoke-test of the CodeRabbit GitHub App + Actions on a PR. Safe to delete.
// Deliberately small, with one edge case for the reviewer to notice.
export function percent(part: number, total: number): number {
  return (part / total) * 100;
}
